const prompt = require ('prompt-sync');
const person = {
    lastname : "laamim",
    firstname : "anas",
    age : 20 ,
}; console.log(`person : ${person.firstname} ${person.lastname} ${person.age}`);