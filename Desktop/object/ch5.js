const prompt = require ('prompt-sync');
const book = {
    title: 'The Great Gatsby',
    writer: 'F. Scott Fitzgerald',
    year: 1925,
}; function addition(title,writer,year){
    return (book)
}console.log(addition(book));